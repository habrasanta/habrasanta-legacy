language: "python"

python:
  - "3.6"

before_script:
  - "npm install -g less yuglify bower"
  - "bower install"
  - "python manage.py collectstatic --noinput --settings=oldsanta.settings"

install: "pip install -r requirements/test.txt"

script:
  - "python manage.py test --settings=oldsanta.settings"
